version: "2"
sql:
  - schema: "db/schema"
    queries: "db/queries"
    engine: "postgresql"
    gen:
      go:
        out: "db/out"
        package: "db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: false
        emit_exact_table_names: false
